(log_perbaikan.md is a markdown file, which is a source/config file, so it should not be added to .gitignore)

Nothing should be ignored based on the provided file changes, as they are all source or config files (.md, .py, .csv).